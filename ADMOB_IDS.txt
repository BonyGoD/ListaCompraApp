# 📝 Configuración de IDs de AdMob

## Instrucciones:
1. Completa este archivo con tus IDs reales de AdMob
2. Guárdalo
3. Avísame y los configuraré automáticamente en el proyecto

---

## 🔒 SEGURIDAD: IDs Protegidos

✅ **Los IDs de producción YA NO están en el código**
✅ **Están en `local.properties` (archivo que NO se sube a Git)**

---

## 📁 Configuración en local.properties

Los IDs de producción están configurados en `local.properties`:

```properties
# AdMob IDs (Producción) - REEMPLAZAR CON TUS NUEVOS IDs
ADMOB_ANDROID_BANNER=ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY
ADMOB_ANDROID_INTERSTITIAL=ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY
ADMOB_IOS_BANNER=ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY
ADMOB_IOS_INTERSTITIAL=ca-app-pub-XXXXXXXXXXXXXXXX/YYYYYYYYYY
```

⚠️ **IMPORTANTE**: `local.properties` está en `.gitignore` y NO se sube al repositorio.

---

## ⚠️ IDs Anteriores Comprometidos

Los IDs que empezaban con `ca-app-pub-2928743687688400` fueron eliminados de AdMob
por haber sido expuestos públicamente en GitHub.

**Ver archivo `NUEVOS_IDS_ADMOB.md` para instrucciones de configuración.**

---

## 👥 Para otros desarrolladores del equipo

Si clonas el repositorio, necesitas crear tu propio `local.properties` con estas líneas:

1. Crea el archivo `local.properties` en la raíz del proyecto
2. Agrega las propiedades de AdMob (solicítales los IDs al propietario)
3. El archivo debe verse así:

```properties
sdk.dir=TU_RUTA_DEL_SDK
FIREBASE_PROJECT_ID=...
FIREBASE_APP_ID=...
FIREBASE_API_KEY=...
CLIENT_ID=...

# AdMob IDs (solicita estos IDs al propietario del proyecto)
ADMOB_ANDROID_BANNER=ca-app-pub-...
ADMOB_ANDROID_INTERSTITIAL=ca-app-pub-...
ADMOB_IOS_BANNER=ca-app-pub-...
ADMOB_IOS_INTERSTITIAL=ca-app-pub-...
```

---

## ✅ Archivos Actualizados (Seguridad)

- ✅ `local.properties` → IDs de producción (NO se sube a Git)
- ✅ `build.gradle.kts` → Lee desde local.properties y genera BuildConfig
- ✅ `AdConstants.kt` → Usa BuildConfig en lugar de hardcodear
- ✅ `.gitignore` → Ya incluye local.properties

---

## 🔍 Verificar Seguridad

Para verificar que los IDs NO se subirán a Git:

```bash
git status
```

`local.properties` NO debe aparecer en la lista de archivos modificados.

---

## 🎯 Modo de Operación

### Actualmente: **PRUEBA** (IDs de Google)
```kotlin
// En AdConstants.kt
private const val USE_TEST_ADS = true
```

### Para producción:
```kotlin
private const val USE_TEST_ADS = false
```

Cuando cambies a `false`, usará los IDs de `local.properties`.


---

## 📍 Dónde encontrar estos IDs:

1. **Ve a**: https://apps.admob.com/
2. **Selecciona tu app** (Android o iOS)
3. **Application ID**:
   - Click en "App settings" (⚙️)
   - Verás "ID de la aplicación" → Cópialo
4. **Ad Unit IDs**:
   - Click en "Ad units"
   - Verás la lista de tus ad units
   - Cada uno tiene un "ID de bloque de anuncios" → Cópialo

---

## ⚠️ IMPORTANTE:

- Los **Application IDs** terminan con `~YYYYYYYYYY` (tienen ~)
- Los **Ad Unit IDs** terminan con `/YYYYYYYYYY` (tienen /)
- NO uses los IDs de prueba, usa TUS IDs reales

---

## ✅ Una vez completado:

Pega tus IDs aquí y avísame. Yo me encargaré de:
- ✅ Configurar AndroidManifest.xml
- ✅ Configurar Info.plist
- ✅ Actualizar AdConstants.kt
- ✅ Verificar que todo compile correctamente
